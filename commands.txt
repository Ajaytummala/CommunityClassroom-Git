1)git init
2)dir= to get the list of folders inside 
3)dir /a=show me hidden files
4)type nul >filename.txt create a new empty file
5)git status=know about the changes in the project
6)git add filename.txt=add the untracked files to tracked files
7)git add .=add all the untracked to tracked files
8)git commit -m =commit the tracked the files like saving the progress taking a snapshot
9)start filename.txt=open the file 
10)type filename.txt=display the content of the file
11)git restore --staged filename.txt=to push back from tracked to untracked files.
12) git log=history of all commits
13)git reset commit id=will remove all the commits above the id and will put in untracked status
14)git stash = will not commit will not loose the changes that u made u can get them whenever u want goes back to previous stages
15)git stash pop = the changes u made will come back
16) git stash clear=will complete erase the files that u hided from commits